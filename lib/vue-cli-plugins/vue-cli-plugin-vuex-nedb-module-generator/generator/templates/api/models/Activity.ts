interface IActivity {
  name: string;
  description: string;
}

const defaultActivity: IActivity = {
  name: "activity title",
  description: "activity description ",
};

export default defaultActivity;
